name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
        python-version: ["3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install test dependencies
        run: python -m pip install -e ".[dev]"

      - name: Run tests
        run: python -m pytest -q

  windows-optional-deps-smoke:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Linux Qt runtime libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libgl1 libxkbcommon-x11-0 libxcb-cursor0

      - name: Install all optional dependencies
        run: python -m pip install -e ".[all,dev]"

      - name: Smoke CLI help
        run: |
          python -m ritualist --help
          python -m ritualist doctor --help
          python -m ritualist home --help
          python -m ritualist inspect-window --help
          python -m ritualist pack --help

  optional-deps-gui-home:
    runs-on: ubuntu-latest
    env:
      QT_QPA_PLATFORM: offscreen
      PYTHONFAULTHANDLER: "1"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install all optional dependencies
        run: python -m pip install -e ".[all,dev]"

      - name: Run optional dependency tests
        run: python -m pytest -q

      - name: Compile package and tests
        run: python -m compileall -q ritualist tests

      - name: Smoke CLI and Home commands
        run: |
          python -m ritualist --help
          python -m ritualist home --help
          python -m ritualist pack --help
          python -m ritualist perf home-model --mock-cards 100 --json
          python -m ritualist perf home-model --mock-cards 300 --json
          python -m ritualist perf fake-run ritualist/sample_recipes/gaming_mode.yaml --json
